<div class="modal fade" id="modalEditPassword" tabindex="-1" aria-labelledby="labelModalEditPassword" aria-hidden="true">
	<div class="modal-dialog modal-dialog-centered modal-lg">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title" id="labelModalEditPassword">Change password</h5>
				<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
			</div>
			<div class="modal-body">
				<div class="card">
                    <div class="card-header pt-4 pb-4 text-center bg-dark">
                        <span><img src="{{ asset('build/images/logo.svg') }}" alt="Logo of over eat" height="50"></span>
                    </div>
                    <div class="card-body p-4">
                        <div class="text-center w-75 m-auto info">
                            <h4 class="text-dark-50 text-center mt-0 fw-bold">Change your password</h4>
                            <p class="text-muted mb-4">You will need to enter the old password and the new one</p>
                        </div>
                        <form name="edit_password_form">
                            <div class="container mt-1 ms-1">
                                <div class="mb-3">
                                    <label class="form-label required" for="edit_password_form_old_password">Old password</label>
                                    <input type="password" id="edit_password_form_old_password" name="edit_password_form[oldPassword]" required="required" autocomplete="off" class="form-control" placeholder="Old password" minlength="8" maxlength="4096"/>
                                </div>
                                <div class="mb-3">
                                    <label class="form-label required" for="edit_password_form_new_password">New password</label>
                                    <input type="password" id="edit_password_form_new_password" name="edit_password_form[newPassword]" required="required" autocomplete="off" class="form-control" placeholder="New password" minlength="8" maxlength="4096"/>
                                </div>
                                <div class="mb-3 text-center">
                                    <button class="btn btn-success" type="submit">Save changes</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
			</div>
		</div>
	</div>
</div>
